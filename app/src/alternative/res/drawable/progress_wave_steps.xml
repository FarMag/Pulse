<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Фоновая линия -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <corners android:radius="50dp"/>
            <solid android:color="#FAE0E0"/> <!-- светло-голубой фон -->
        </shape>
    </item>

    <!-- Заполненная волна -->
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <corners android:radius="50dp"/>
                <gradient
                    android:angle="270"
                    android:startColor="#F74F4F"
                    android:endColor="#D10202"
                    android:type="linear"/>
            </shape>
        </clip>
    </item>
</layer-list>
